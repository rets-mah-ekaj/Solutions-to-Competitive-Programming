#include<bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    long a, b, SOMA;
    cin >> a >> b;
    SOMA = a + b;
    cout << "SOMA = " << SOMA << '\n';
    return 0;
}
