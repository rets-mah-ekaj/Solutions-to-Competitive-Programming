#include<bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int n, x, val, no = 0, give = 0;
  char ch;
  cin >> n >> x;
  while (n--) {
    cin >> ch >> val;
    if (ch == '+') {
      
    }
  }
}
