#include<bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int t, n, m, x, y, a, b;
  cin >> t;
  while (t--) {
    cin >> n >> m >> x >> y >> a >> b;
    if (x == n && y + 1 == m)
      cout << "YES\n";
    else if (y == m && x + 1 == n)
      cout << "YES\n";
    else if ()
  }
}
