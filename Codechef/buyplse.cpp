#include<bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int a, b, x, y;
  cin >> a >> b >> x >> y;
  cout << a * x + b * y;
  return 0;
}
