#include<bits/stdc++.h>
uisng namespace std;

void dfs(int cur, int par) {
  dp[cur] = 1, total[cur] = 1;
  long long sum = 0, count = 0;
  for (int i = 0; i < )
}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int t;
  cin >> t;
  while (t--) {
    int ans = 0;
    int n;
    cin >> n;

  }
}
