#include<bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int t, D, d, p, q;
  cin >> t;
  while (t--) {
    cin >> D >> d >> p >> q;
    
  }
  return 0;
}
